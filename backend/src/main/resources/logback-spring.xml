<?xml version="1.0" encoding="UTF-8"?>
<!--
  P-08 修复：Logback 配置
  - 主应用日志输出到 logs/exam.log，按天滚动，保留 30 天
  - 审计日志（audit logger）单独输出到 logs/audit.log，保留 90 天，便于合规审计
  - 控制台输出保持简洁，仅 INFO 及以上级别
-->
<configuration>
    <property name="LOG_PATH" value="${LOG_PATH:-logs}" />
    <property name="LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n" />

    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 主应用日志文件，按天滚动 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/exam.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/exam.%d{yyyy-MM-dd}.log.gz</fileNamePattern>
            <maxHistory>30</maxHistory>
            <totalSizeCap>5GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- P-08 修复：审计日志文件，单独输出到 audit.log，保留 90 天 -->
    <appender name="AUDIT_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/audit.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/audit.%d{yyyy-MM-dd}.log.gz</fileNamePattern>
            <maxHistory>90</maxHistory>
            <totalSizeCap>10GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 审计 logger：仅输出到 AUDIT_FILE，不向上传递到 root -->
    <logger name="audit" level="INFO" additivity="false">
        <appender-ref ref="AUDIT_FILE" />
    </logger>

    <!-- 应用日志 -->
    <logger name="lsgwr.exam" level="${EXAM_LOG_LEVEL:-info}" />

    <!-- Hibernate SQL 参数绑定关闭，避免敏感数据泄露（A-08） -->
    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="OFF" />

    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>
</configuration>
